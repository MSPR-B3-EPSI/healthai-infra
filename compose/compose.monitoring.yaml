services:
  prometheus:
    image: prom/prometheus:v2.55.1
    profiles: ["monitoring"]
    # Reverse proxy via cloudflared (sous-domaine dédié). Prometheus n'a pas d'env
    # vars équivalentes, on passe par des flags CLI. Sur le Pi `.env` :
    #   PROMETHEUS_EXTERNAL_URL=https://prometheus.healthai.chomizzz.fr
    #   PROMETHEUS_ROUTE_PREFIX=/   ← défaut, pas de préfixe puisque sous-domaine
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.external-url=${PROMETHEUS_EXTERNAL_URL:-http://localhost:9090}
      - --web.route-prefix=${PROMETHEUS_ROUTE_PREFIX:-/}
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ../monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    networks:
      - app

  loki:
    image: grafana/loki:3.2.1
    profiles: ["monitoring"]
    command: -config.file=/etc/loki/local-config.yaml
    ports:
      - "${LOKI_PORT:-3100}:3100"
    volumes:
      - ../monitoring/loki/loki-config.yaml:/etc/loki/local-config.yaml:ro
    networks:
      - app

  grafana:
    image: grafana/grafana:11.2.2
    profiles: ["monitoring"]
    environment:
      # Reverse proxy via cloudflared (sous-domaine dédié). Sur le Pi `.env` :
      #   GF_SERVER_ROOT_URL=https://grafana.healthai.chomizzz.fr
      #   GF_SERVER_SERVE_FROM_SUB_PATH=false   ← défaut, sous-domaine
      GF_SERVER_ROOT_URL: ${GF_SERVER_ROOT_URL:-http://localhost:3001}
      GF_SERVER_SERVE_FROM_SUB_PATH: ${GF_SERVER_SERVE_FROM_SUB_PATH:-false}
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ../monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ../monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - app

  promtail:
    image: grafana/promtail:3.2.1
    profiles: ["monitoring"]
    user: "0:0"
    command: -config.file=/etc/promtail/config.yml
    volumes:
      - ../monitoring/promtail/promtail-config.yaml:/etc/promtail/config.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - promtail_positions:/run/promtail
    depends_on:
      - loki
    networks:
      - app

networks:
  app:

volumes:
  grafana_data:
  promtail_positions:
