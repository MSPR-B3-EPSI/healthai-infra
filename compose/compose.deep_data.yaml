services:
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    profiles: ["deep-data"]
    environment:
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-change-me}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-change-me}
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ../deep-data/clickhouse/init:/docker-entrypoint-initdb.d:ro
      - ../deep-data/clickhouse/config.d:/etc/clickhouse-server/config.d:ro
      - ../deep-data/clickhouse/users.d:/etc/clickhouse-server/users.d
    ports:
      - "${CLICKHOUSE_HTTP_PORT:-8123}:8123"
      # Default 9002 (et non 9000) pour éviter le conflit avec Authentik/Portainer
      # qui squattent souvent le 9000 host. Le port INTERNE reste 9000.
      - "${CLICKHOUSE_NATIVE_PORT:-9002}:9000"
    ulimits:
      nofile: { soft: 262144, hard: 262144 }
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app

  minio:
    image: minio/minio:latest
    profiles: ["deep-data"]
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-change-me}
      # Reverse proxy : la console MinIO doit savoir son URL publique pour
      # générer les bons redirects + les liens vers les assets statiques.
      # Override sur le serveur staging via `.env` :
      #   MINIO_BROWSER_REDIRECT_URL=https://healthai.chomizzz.fr/minio
      MINIO_BROWSER_REDIRECT_URL: ${MINIO_BROWSER_REDIRECT_URL:-http://localhost:9001}
    volumes:
      - minio_data:/data
    ports:
      - "${MINIO_API_PORT:-9100}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    networks:
      - app

networks:
  app:

volumes:
  clickhouse_data:
  minio_data:
